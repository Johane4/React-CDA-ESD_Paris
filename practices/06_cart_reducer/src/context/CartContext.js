import { createContext } from "react";

/*
  COURS :
  Le Context sert à PARTAGER le panier
  entre plusieurs composants (produits, header, page panier…)
*/

export const CartContext = createContext();
